  <div class="char-ability-title">{{this.title}}{{#if this.rating}} ({{this.rating}}){{/if}}</div>
  <table class="char-ability-list">
  {{#each this.list as |ability| }}
  <tr>
    <td>{{ability.name}} 
   </td>
    <td>{{ability.rating}}
   </td>
  </tr>
  {{/each}}
</table>
