<BsModalSimple @title="Set Damage" @closeTitle="Cancel" @submitTitle="Set Damage" @size={{null}} @fade={{true}} @open={{this.selectDamage}} @onSubmit={{this.setDamage}} @onHide={{this.cancelDamage}}>

    <p><b>Set Damage</b></p>
       <p>Select a character and a wound level, or type the info into the boxes.</p>

       <br>Character: 
       {{#if this.scene}}
       <select onchange={{this.changeNameString}}>
       {{#each this.scene.participants as |char|}}
           <option value={{char.name}}>{{char.name}}</option>
       {{/each}}
       </select>
       {{/if}}
    <Input @type="text" size={{25}} id="nameString" @enter={{this.setDamage}} @value={{this.nameString}} />

     <br>Wound Level:
       <select onchange={{this.changeDamageString}}>
       {{#each this.woundLevels as |level|}}
           <option value={{level}}>{{level}}</option>
       {{/each}}
       </select>
    <Input @type="text" size={{25}} id="damageString" @enter={{this.setDamage}} @value={{this.damageString}} />

 </BsModalSimple>
